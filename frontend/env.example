# AWS API Gateway URL
# Get this from CDK deployment output: TodoApiStack.ApiUrl
# Example: https://abc123def4.execute-api.ca-central-1.amazonaws.com/prod
NUXT_PUBLIC_API_BASE_URL=https://your-api-gateway-url.execute-api.ca-central-1.amazonaws.com/prod

# ============================================================================
# HOW TO GET THIS VALUE:
# ============================================================================
# 
# 1. Navigate to your aws-backend directory:
#    cd ../aws-backend
#
# 2. Deploy the stacks (if not already deployed):
#    npm run build
#    cdk deploy --all
#
# 3. After deployment, the CDK will output the API URL:
#    - TodoApiStack.ApiUrl -> Use for NUXT_PUBLIC_API_BASE_URL
#
# 4. Alternatively, get the value from AWS Console:
#    - API Gateway URL: AWS Console > API Gateway > TodoAppApi > Stages > prod
#
# 5. Copy this file to .env and replace the placeholder value:
#    cp env.example .env
#
# ============================================================================
# PASSWORD REQUIREMENTS:
# ============================================================================
# - Minimum 8 characters
# - At least one uppercase letter
# - At least one lowercase letter
# - At least one number
#
# Note: If you get "Password does not meet requirements" error during signup,
# ensure your password follows these rules.
# ============================================================================

